const $=s=>document.querySelector(s);const labels={inicio:['Actividad','Vista general de la jornada.'],trabajos:['Trabajos','Agenda compartida y asignaciones.'],partes:['Partes','Instalación, reparación y mantenimiento.'],instalaciones:['Instalaciones','Ficha única de cliente, equipo e historial.'],mapa:['Mapa','Ubicaciones protegidas y pendientes.'],gestion:['Gestión','Intervenciones, informes y administración.']};
function net(){const on=navigator.onLine;$('#netText').textContent=on?'Conectado':'Sin conexión';$('#netDot').style.background=on?'#27ae60':'#eb5757'}addEventListener('online',net);addEventListener('offline',net);net();
function go(k){document.querySelectorAll('.bottom button').forEach(b=>b.classList.toggle('active',b.dataset.go===k));const [t,d]=labels[k]||labels.inicio;$('#content').innerHTML=`<div class="panel-head"><div><p class="eyebrow">Módulo</p><h3>${t}</h3></div><span class="pill">v0.1</span></div><p>${d}</p><p class="note">En la siguiente iteración este módulo incorporará la pantalla familiar de la aplicación actual y se conectará al modelo común de datos.</p>`}document.querySelectorAll('[data-go]').forEach(b=>b.addEventListener('click',()=>go(b.dataset.go)));
$('#role').addEventListener('change',e=>{$('#welcome').textContent=e.target.value==='tecnico'?'Buenas, técnico':e.target.value==='gestion'?'Panel de gestión':'Buenas, Oliver'});
if('serviceWorker'in navigator)navigator.serviceWorker.register('./sw.js').catch(()=>{});
